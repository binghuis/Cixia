import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("设置")
                .font(.title)
            // 这里可以添加更多设置项
        }
        .frame(width: 400, height: 300)
        .padding()
    }
} 